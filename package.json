{
  "name": "ashish-portfolio",
  "type": "module",
  "version": "0.0.6",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run clean && astro build && echo > dist/.nojekyll && gh-pages -d dist",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.13.3",
    "astro-icon": "^1.1.5",
    "gh-pages": "^6.3.0"
  }
}
